package Package03OOP;

public class OOPPackage {
}

/**
 * 小结：
 * java内建的 package机制是为了避免 class命名冲突
 * jdk的核心类使用 java.lang包，编译器会自动导入
 * jdk的其他常用类定义在 java.util.*, java.math.*, java.text.*
 * 包名推荐使用倒置的域名，例如：org.apache
 */

