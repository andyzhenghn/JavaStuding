package Package03OOP;

public class OOPClasspath {
}

/**
 * jvm通过环境变量 classpath决定搜索 class的路径和顺序
 * 不推荐设置环境变量 classpath，始终建议通过 -cp命令传入
 * jar包相当于目录，可以包含很多 .class文件，方便下载和使用
 * MANIFEST.MF文件可以提供 jar包的信息，如 Main-Class，这样可以直接运行 jar包
 */
