package com.study.LXF01.Package15Maven;

/**
 * Maven就是专门为 java项目打造的管理和构建工具
 * 提供了一套标准化的项目结构
 * 提供了一套标准化的构建流程（编译，测试，打包，发布...）
 * 提供了一套依赖管理机制
 *
 * 设置几个环境变量
 * export M2_HOME=/path/to/maven-3.6.x
 * export PATH=$PATH:$M2_HOME/bin
 */

public class Introduction {
}

/**
 * 小结
 * Maven是一个 java项目的管理和构建工具
 *  Maven使用 pom.xml定义项目内容，并使用预设的目录结构
 *  在 Maven中声明一个依赖项可以自动下载并导入 classpath
 *  Maven使用 groupId, artifactId和 version唯一定位一个依赖
 */
